public class Cell {
    // Attributs
    private Position position;
    private boolean isEmply;

    public Cell(Position position, boolean isEmply){
        this.position = position;
        this.isEmply = isEmply;
    }
}
