import java.util.Scanner;
public class Player{
        private String name;
        private int color;

        // Constructeur de player
        public Player(String name, int color)
        {
                this.name = name;
                this.color = color;
        }

}